import React from 'react';

import FormControlRange from './FormControlRange';

export default {
  component: FormControlRange,
  title: 'Form Control: Range input option',
  excludeStories: /.*Data$/,
};

export const Default = () => <FormControlRange />;